{% extends 'base.html.twig' %}

{% block title %}Movimientos{% endblock %}

{% block body %}
    <h1>Movimientos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movimiento.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ movimiento.tipo }}</td>
            </tr>
            <tr>
                <th>Monto</th>
                <td>{{ movimiento.monto }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ movimiento.date ? movimiento.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_movimientos_index') }}">back to list</a>

    <a href="{{ path('app_movimientos_edit', {'id': movimiento.id}) }}">edit</a>

    {{ include('movimientos/_delete_form.html.twig') }}
{% endblock %}
