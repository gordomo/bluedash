<form method="post" action="{{ path('app_pool_delete', {'id': pool.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pool.id) }}">
    <button class="btn">Delete</button>
</form>
