{% extends 'base.html.twig' %}

{% block title %}Participaciones{% endblock %}

{% block body %}
    <h1>Participaciones</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participacione.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ participacione.fecha ? participacione.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Monto</th>
                <td>{{ participacione.monto }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ participacione.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_participaciones_index') }}">back to list</a>

    <a href="{{ path('app_participaciones_edit', {'id': participacione.id}) }}">edit</a>

    {{ include('participaciones/_delete_form.html.twig') }}
{% endblock %}
